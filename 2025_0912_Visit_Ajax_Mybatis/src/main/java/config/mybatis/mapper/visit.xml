<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="visit">

<!-- 전체조회 --> <!-- sqlMapConfig에서 typeAlias 설정 --><!-- db.vo.VisitVo = visit -->
 <select id="visit_list" resultType="visit">
 	select * from visit order by idx desc
 </select>
 
<!-- 방명록 조건별 조회 -->
<select id="visit_list_condition" resultType="visit" parameterType="Map">
 	select * from visit
 	<trim prefix="where" prefixOverrides="or">
 		<if test="name!=null">
 			name like '%' || #{ name } || '%'
 		</if>
 		
 		<if test="content!=null">
 			or content like '%' || #{ content } || '%'
 		</if>
 	</trim>
 	order by idx desc
</select>

 <!-- 하나 조회 -->
<select id="visit_one" resultType="visit">
	select * from visit where idx = #{ idx }
</select>


 <!-- 추가  -->
 <insert id="visit_insert" parameterType="visit">
 	insert into visit values( seq_visit_idx.nextVal,
 	 						#{ name }, #{ content }, #{ pwd }, #{ ip }, sysdate )
 </insert>
  
 <!-- 삭제 -->
 <delete id="visit_delete" parameterType="int">
 	delete from visit  where idx = #{ idx }
 </delete>
 

<!-- 수정 -->
<update id="visit_update" parameterType="visit">
	update visit set name=#{name},content=#{ content },
						pwd=#{ pwd },ip=#{ ip }  where idx=#{idx}
</update>

</mapper>












